namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $.$hyoo_idea_person_card, {

		alignItems: 'center',

		Actions: {
			margin: {
				left: 'auto',
			},
		},

		Name_link: {
			display: 'inline-flex',
			padding: 0,
		},

		Avatar: {
			width: rem(3),
			height: rem(3),
		},
		
	} )

}
