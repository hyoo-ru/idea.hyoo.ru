namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $hyoo_idea_sign_up_page, {

		margin: [ rem(5), 'auto' ],
		flex: {
			basis: rem(30),
			grow: 0,
		},

		background: {
			color: 'unset',
		},

	} )

}
